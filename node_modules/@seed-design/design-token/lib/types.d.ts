import * as vars from './vars';
declare type LooseValueType<T> = (T extends object ? {
    readonly [K in keyof T]: LooseValueType<T[K]>;
} : T extends string ? string : never);
export declare type TokenObject = LooseValueType<typeof vars>;
export {};
