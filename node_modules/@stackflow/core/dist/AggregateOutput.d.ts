import type { PushedEvent, ReplacedEvent, StepPushedEvent, StepReplacedEvent } from "./event-types";
export declare type ActivityTransitionState = "enter-active" | "enter-done" | "exit-active" | "exit-done";
export declare type ActivityStep = {
    id: string;
    params: {
        [key: string]: string | undefined;
    };
    pushedBy: PushedEvent | ReplacedEvent | StepPushedEvent | StepReplacedEvent;
};
export declare type Activity = {
    id: string;
    name: string;
    transitionState: ActivityTransitionState;
    params: {
        [key: string]: string | undefined;
    };
    context?: {};
    pushedBy: PushedEvent | ReplacedEvent;
    steps: ActivityStep[];
    isTop: boolean;
    isActive: boolean;
    zIndex: number;
};
export declare type RegisteredActivity = {
    name: string;
    paramsSchema?: {};
};
export declare type AggregateOutput = {
    activities: Activity[];
    registeredActivities: RegisteredActivity[];
    transitionDuration: number;
    globalTransitionState: "idle" | "loading";
};
