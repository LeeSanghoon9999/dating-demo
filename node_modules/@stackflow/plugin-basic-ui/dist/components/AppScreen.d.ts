import React from "react";
import type { GlobalVars } from "../theme.css";
import type { PropOf } from "../utils";
import AppBar from "./AppBar";
declare type AppScreenProps = Partial<Pick<GlobalVars, "backgroundColor" | "dimBackgroundColor">> & {
    appBar?: Omit<PropOf<typeof AppBar>, "theme" | "ref" | "key">;
    children: React.ReactNode;
};
declare const AppScreen: React.FC<AppScreenProps>;
export default AppScreen;
