import type { ActivityComponentType, StackflowReactPlugin } from "@stackflow/react";
import type { Loader } from "./Loader";
export declare type PreloadPluginOptions<T extends {
    [activityName: string]: unknown;
}> = {
    loaders: {
        [key in Extract<keyof T, string>]?: T[key] extends ActivityComponentType<infer U> ? Loader<U> : Loader<{}>;
    };
};
export declare function preloadPlugin<T extends {
    [activityName: string]: unknown;
}>(options: PreloadPluginOptions<T>): StackflowReactPlugin<T>;
