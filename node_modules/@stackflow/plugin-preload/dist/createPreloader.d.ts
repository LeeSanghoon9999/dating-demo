import type { PreloadFunc } from "./usePreloader";
export declare function createPreloader<T extends {
    [activityName: string]: unknown;
}>(): {
    usePreloader: () => {
        preload: PreloadFunc<T>;
    };
};
